/* eslint-disable */
import { toRefs, watch, reactive } from 'vue';


export default {
    props: {
        schema: Object,
        formData: Object
    },

    setup(props, { emit }) {
        
    }
}